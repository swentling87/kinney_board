<section>
  <div class="jumbotron">
    <h2>Welcome to the Message Board</h2>
    <% if user_signed_in? %>
      <%= link_to "See Posts", posts_path, class: 'btn btn-primary btn-lg' %>
    <% else %>
      <%= link_to "Sign Up", new_user_registration_path, class: 'btn btn-success btn-lg' %>
      <%= link_to "Sign In", new_user_session_path, class: 'btn btn-primary btn-lg' %>
    <% end %>
  </div>
</section>
